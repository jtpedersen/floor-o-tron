---

duty_cycle_controller:
  module: duty_cycle_controller
  class: DutyCycleController
  log_level: !env_var log_level
  heater_switch: !env_var heater_switch
  duty_cycle_percentage: !env_var duty_cycle_percentage
  min_pulse_width: !env_var min_pulse_width
  history_duration: !env_var history_duration
  adjustment_interval: !env_var adjustment_interval
